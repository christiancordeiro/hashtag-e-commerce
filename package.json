{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss build -i styles.css -o src/styles.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  }
}
